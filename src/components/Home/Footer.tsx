'use client'
import { Breadcrumb, Layout, Menu, theme } from 'antd';

const { Header, Content, Footer, Sider } = Layout;

const FooterPage = () => {
  return (
    <div>
     <Footer style={{ textAlign: 'center' }}>Teaching Torch ©2023. Build To Better</Footer>
    </div>
  );
};

export default FooterPage;